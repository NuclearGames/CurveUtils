using System.Numerics;

namespace CurvesWebEditor.Data.CanvasRendering {
    public sealed class CanvasDrawer {
        private readonly CanvasRenderContext _context;

        public CanvasDrawer(CanvasRenderContext context) {
            _context = context;
        }

        internal Vector2 Point(Vector2 point) {

        }
    }
}
